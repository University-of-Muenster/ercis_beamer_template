% Shortcut to render the final page
\newcommand{\finalpage}{%
  \usebeamertemplate{final page}%
}

% Set final page template
\setbeamertemplate{final page}{%
  \tikzsetnextfilename{ercis-final}%
  %
  % Undo space reservation of text margin and sidebar width
  \hspace*{-\leftTextMargin}%
  \hspace*{-\leftSidebarWidth}%
  \begin{tikzpicture}

    % Picture size and background
    \usebeamercolor{final page background}
    \draw [draw = none, shading = axis, left color={final page background.bg}, right color={final page background.fg}, even odd rule] (0mm, 0mm) rectangle (\paperwidth, \paperheight)
    (.125\paperwidth, .125\paperheight) rectangle (.875\paperwidth, .875\paperheight);

    % Draw background
    \draw [draw = none, fill = white, blur shadow={%
        {shadow xshift=1.5mm, shadow yshift=-1.5mm, shadow scale=1.02, shadow blur radius=4mm, %
        shadow blur steps = 10, shadow blur extra rounding}}]%
        (.125\paperwidth, .125\paperheight) rectangle (.875\paperwidth, .875\paperheight);

    % % ERCIS logo
    \node[anchor=north east, no sep] at ({.875\paperwidth - \textMarginFinal}, {.875\paperheight - 6mm}) {%
      \resizebox{!}{32mm}{\ercislogotext{ercisred}{ercisblue}{ercisred}{ercisblue}}%
    };

    % University logo
    \node[anchor=north west, no sep] at ({.125\paperwidth + \textMarginFinal}, {.875\paperheight - 6mm}) {%
      \resizebox{!}{10mm}{\wwulogo{{final page wwu logo.fg}}}%
    };

  \end{tikzpicture}%
  % Undo space reservation by cached final part
  \hspace*{-\paperwidth}%
  \tikzset{external/export next=false}%
  \begin{tikzpicture}
    % Picture size
    \path [use as bounding box] (0mm, 0mm) rectangle (\paperwidth, \paperheight);

    % Content
    \ifbeamertemplateempty{final page contact}{}{%
      \node[anchor=text, no sep, text width={.75\paperwidth - \textMarginFinal - \textMarginFinal}] at ({.125\paperwidth + \textMarginFinal}, {.4\paperheight}) {%
        \usebeamerfont{final page contact}%
        \usebeamercolor[fg]{final page contact}%
        \usebeamertemplate{final page contact}%
      };
    }

    % Contact details (optional)
    \ifbeamertemplateempty{final page contactdetails}{}{%
      \node[anchor=text, no sep, align=flush left, text width={.75\paperwidth - \textMarginFinal - \textMarginFinal}] at ({.125\paperwidth + \textMarginFinal}, {.4\paperheight - 6mm}) {%
        \usebeamerfont{final page contactdetails}%
        \usebeamercolor[fg]{final page contactdetails}%
        \usebeamertemplate{final page contactdetails}%
      };
    }
  \end{tikzpicture}%
}

