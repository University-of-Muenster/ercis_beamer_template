% Shortcut to render the final page
\newcommand{\finalpage}{%
  \usebeamertemplate{final page}%
}

% Set final page template
\setbeamertemplate{final page}{%
  \vspace*{-2mm}% TODO Why is this needed?
  \tikzsetnextfilename{ercis-final}%
  %
  % Undo space reservation of text margin and sidebar width
  \hspace*{-\leftTextMargin}%
  \hspace*{-\leftSidebarWidth}%
  \begin{tikzpicture}
    % Picture size and background
    \usebeamercolor{final page background}
    \path [fill, color={final page background.bg}, use as bounding box] (0mm, 0mm) rectangle (\paperwidth, \paperheight);

    % % ERCIS logo
    \node[anchor=north east, no sep] at ({\paperwidth - 6.75mm}, {\paperheight - 6mm}) {%
      \resizebox{!}{22.5mm}{\ercislogotext{white}{white}{white}{white}}%
    };

    % Claim
    \node[anchor=south west, no sep] at (6mm, 7mm) {%
      \resizebox{!}{4.5mm}{\ercisclaim}%
    };

    % URL
    \node[anchor=south east, no sep] at ({\paperwidth - 6.75mm}, 7mm) {%
      \usebeamerfont{final page url}%
      \usebeamercolor[fg]{final page url}%
      www.ercis.org
    };
  \end{tikzpicture}%
  % Undo space reservation by cached final part
  \hspace*{-\paperwidth}%
  \tikzset{external/export next=false}%
  \begin{tikzpicture}
    % Picture size
    \path [use as bounding box] (0mm, 0mm) rectangle (\paperwidth, \paperheight);

    % Content
    \ifbeamertemplateempty{final page content}{}{%
      \node[anchor=text, no sep, text width={\paperwidth - \leftTextMargin - \rightTextMargin}] at (6mm, {\paperheight / 2}) {%
        \usebeamerfont{final page content}%
        \usebeamercolor[fg]{final page content}%
        \usebeamertemplate{final page content}%
      };
    }

    % Contact details (optional)
    \ifbeamertemplateempty{final page contact}{}{%
      \node[anchor=text, no sep, align=left, text width={\paperwidth - \leftTextMargin - \rightTextMargin}] at (6mm, {\paperheight - 75mm}) {%
        \usebeamerfont{final page contact}%
        \usebeamercolor[fg]{final page contact}%
        \usebeamertemplate{final page contact}%
      };
    }
    % Undo space reservation by prior final part
    \hspace*{-\paperwidth}%
    Test
  \end{tikzpicture}%
}

