% Declare name of the package
\ProvidesPackage{ercisbeamer}

% Better font rendering (T1 font encoding)
\RequirePackage[T1]{fontenc}

% Improves general appearance of text
\RequirePackage{microtype}

% TikZ picture rendering
\RequirePackage{tikz}

% Require geometry package
\RequirePackage{geometry}

% Color management
\RequirePackage{color}

% Small square bullets for itemize
\RequirePackage{MnSymbol}

% Color minin environment
\RequirePackage{xxcolor}

% Conditional commands
\RequirePackage{ifthen}

% Advanced resizing of contents
\RequirePackage{adjustbox}

% Allow caching of TikZ graphics (externalize feature)
\usetikzlibrary{external}

% Folder for cached pictures (depends on current mode)
\ifthenelse{\equal{\tikzexternal@filenameprefix}{}}{%
    \tikzsetexternalprefix{beamer-cache/}

    \mode<handout>{
      \tikzsetexternalprefix{handout-cache/}
    }

    \mode<article>{
      \tikzsetexternalprefix{article-cache/}
    }
}{}

% Only named pictures are cached
% \tikzset{external/only named=true}

